<% content_for :page_title do %>
  <%= Spree.t(:new_newsletter) %>
<% end %>

<div data-hook="admin_newsletter_new_form_header">
  <%= render 'spree/shared/error_messages', target: @newsletter %>
</div>

<div data-hook="admin_newsletter_new_form">
  <%= form_for [:admin, @newsletter] do |f| %>
    <div class="row">
      <div class="col-md-12">
        <%= render 'form', f: f %>
        <%= render 'spree/admin/shared/new_resource_links' %>
      </div>
    </div>
  <% end %>
</div>
